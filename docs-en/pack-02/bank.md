# Question Bank — Pack 02

Express question list for voice rehearsal and self-testing.

1. Why do we separate a Dockerfile into `builder` and `runtime` stages?
2. Why is running containers as `root` a critical security hazard?
3. What happens when you deploy with `:latest` during an emergency rollback?
4. How do you securely inject SSH keys into GitLab CI pipelines?
5. What is the role of `HEALTHCHECK` in Docker and what states does it report?
6. What actions do you take in the first 60 seconds of a broken release?
7. What are the three mandatory fields in PR Conventions (Rule #2)?
8. What is the purpose of the `Host` and `X-Forwarded-For` headers in Nginx?
9. Explain the DORA framework and the MTTR metric.
10. What does an incoming TCP packet with the `RST` flag indicate during a handshake?
