# Вопросы с собеседований — Пак 02

Эти вопросы задают на техническом собеседовании на позицию Junior+/Middle DevOps/SRE.

---

## Docker и Контейнеризация
1. Как устроен процесс multi-stage сборки в Docker и какую проблему безопасности он решает?
2. В чем разница между инструкциями `COPY` и `ADD` в Dockerfile?
3. Что такое PID 1 внутри контейнера, почему процесс должен уметь обрабатывать сигнал `SIGTERM` и что такое «зомби-процессы»?
4. Чем `CMD` отличается от `ENTRYPOINT` в Dockerfile?
5. Как работает инструкция `HEALTHCHECK` и какие статусы (`starting`, `healthy`, `unhealthy`) может принимать контейнер?

---

## CI/CD и Эксплуатация
6. Почему использование тега `:latest` в продакшн-пайплайнах считается антипаттерном?
7. Как безопасно хранить и передавать SSH-ключи и пароли к базам данных в GitLab CI? Что делают флаги Protected и Masked?
8. Как организовать процедуру быстрого отката (rollback) в случае неудачного релиза?
9. Что такое четыре метрики DORA (Deployment Frequency, Lead Time, Change Failure Rate, MTTR) и как их оптимизировать?
10. Что такое иммутабельная инфраструктура и почему нельзя вносить ручные правки на сервере?

---

## Сети и Веб-серверы
11. Что такое обратный прокси (Reverse Proxy) и зачем он ставится перед сервером приложений?
12. Зачем нужны заголовки `Host`, `X-Real-IP`, `X-Forwarded-For` и `X-Forwarded-Proto` в конфигурации Nginx?
13. Что такое трёхстороннее рукопожатие TCP (three-way handshake)? Что происходит, если порт закрыт (флаг `RST`)?
14. Какой командой в Linux можно за пару секунд проверить открытые слушающие порты процесса (`ss -tulpn`)?
15. Что происходит при получении контейнером сигнала `SIGKILL` по сравнению с `SIGTERM`?
