# Банк вопросов — Пак 02

Список экспресс-вопросов для тренировки артикуляции перед зеркалом или диктофоном.

1. Зачем разделять Dockerfile на `builder` и `runtime`?
2. Почему опасно запускать контейнеры от `root`?
3. Чем грозит использование тега `:latest` при откате релиза?
4. Как безопасно передать SSH-ключ в пайплайн GitLab CI?
5. Что такое `HEALTHCHECK` в Docker и какие статусы у него бывают?
6. Что делать в первые 60 секунд, если релиз сломал прод?
7. Какие 3 обязательных поля требует Правило №2 (PR Conventions)?
8. Зачем нужны заголовки `Host` и `X-Forwarded-For` в Nginx?
9. Что такое четырнадцать секунд DORA и метрика MTTR?
10. Что означает пакет с флагом `RST` в сетевом рукопожатии?
