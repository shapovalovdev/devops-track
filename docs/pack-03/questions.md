# Вопросы с собеседований — Пак 03

Эти вопросы задают на техническом собеседовании на позицию Junior+/Middle DevOps/SRE.

---

## Сети и сокеты в Linux
1. Какой командой посмотреть список открытых портов и процессов, которые их занимают? Чем `ss` отличается от `netstat`?
2. Что означает адрес `0.0.0.0` в выводе `ss` или `netstat`? Чем он отличается от `127.0.0.1`?
3. Что произойдет, если два процесса попытаются сделать `bind` на один и тот же порт и один и тот же IP-адрес?
4. Как посмотреть таблицу маршрутизации в Linux? Какой командой выяснить, через какой интерфейс пойдет пакет до `1.1.1.1`?
5. Что такое default gateway (шлюз по умолчанию) и почему добавление второго сетевого интерфейса может оборвать доступ в интернет?
6. Что такое трёхстороннее рукопожатие TCP (three-way handshake)? Что означает, если на запрос возвращается пакет с флагом `RST`?

---

## Prometheus и Мониторинг
7. В чем разница между Pull и Push моделями сбора метрик? В чем преимущества подхода Prometheus?
8. Какие основные типы метрик есть в Prometheus? Приведите примеры использования Counter и Gauge.
9. Как Prometheus рассчитывает темп прироста метрики с помощью функции `rate()`? Что происходит при перезапуске сервиса, когда Counter сбрасывается в ноль?
10. Чем отличается `node_exporter` от `cAdvisor`?
11. Что такое High Cardinality (высокая кардинальность) в лейблах метрик Prometheus и почему опасно помещать user_id или email в лейблы?
12. Как устроен эндпоинт `/metrics`? Какой протокол используется для сбора метрик?

---

## CI/CD и Продакшн-практики
13. Почему использование тега `:latest` в пайплайнах сборки и доставки считается антипаттерном?
14. Как организовать процедуру быстрого отката (rollback) в случае неудачного деплоя?
15. Что такое иммутабельная инфраструктура и иммутабельные артефакты доставки?
