# Банк вопросов — Пак 03

Список экспресс-вопросов для тренировки артикуляции перед зеркалом или диктофоном.

1. `ss -tulpn` — расшифруй каждый флаг за 5 секунд.
2. `lsof -i :80` — что выведет команда?
3. `ip route get 8.8.8.8` — что покажет ядро?
4. Зачем в Prometheus нужен `scrape_interval`?
5. В чем разница между `rate(http_requests_total[5m])` и `increase(http_requests_total[5m])`?
6. Что такое метрика `up` и какие значения она принимает?
7. Чем опасен тег `:latest` при откате релиза?
